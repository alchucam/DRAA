{
  "name": "server",
  "version": "1.0.0",
  "description": "server side of draa",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "heroku-postbuild": "cd client/ && npm install && npm run build"
  },
  "author": "Alex Chu",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "mysql": "^2.16.0"
  },
  "engines":{
    "node":"8.12.0"
  }
}
